meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "teste2@teste.com",
    "password": "senha123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
